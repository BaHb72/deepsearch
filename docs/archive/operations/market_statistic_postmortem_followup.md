# deepsearch 市场统计分支 Postmortem 跟踪

| 编号 | 问题摘要                                       | 当前状态                                    | 代码依据                                                                                                                                                                                                              | 后续计划                                         |
|----|--------------------------------------------|-----------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------|
| 1  | `AmazingData query_snapshot` 返回空 `Mapping` | ✅ 已通过进程隔离层统一修正：查询前会自动校准交易日区间并确保会话有效     | `deepsearch/infrastructure/providers/implementations/amazingdata/amazingdata_process.py:464`、`:741`、`:1232`                                                                                                       | 继续监控 `Mapping(len=0)` 告警，如 7 天内无复现则关闭对应监控项   |
| 2  | `d.a.m.runner:72` 3 秒超时导致实时查询失败            | ✅ 已切换为订阅流 + 缓冲读取模式，不再走轮询快照              | `deepsearch/application/market_data/service.py:53`、`:185`，`deepsearch/application/market_data/pipeline.py:43`，`deepsearch/infrastructure/providers/implementations/amazingdata/market_stream_adapter.py:130`      | 每日例行巡检订阅健康指标（滞后、掉线次数），必要时扩展自检脚本              |
| 3  | `amazingdata_process_proxy` 频繁重启子进程        | ✅ 新增登录节流与进程池复用机制，默认使用 `spawn` 避免继承失效会话  | `deepsearch/infrastructure/providers/implementations/amazingdata/amazingdata_process_pool.py:71`、`:387`、`:430`，`deepsearch/infrastructure/providers/implementations/amazingdata/amazingdata_process_proxy.py:220` | 保持对 `Worker process started` 日志的告警阈值（>3 次/日） |
| 4  | `boards=沪深,创业板` 维度缺失                       | ✅ 股票清单归一化后补齐板块元数据，并在领域层扩展映射             | `deepsearch/infrastructure/providers/implementations/amazingdata/amazingdata.py:335`、`:430`、`:475`、`:2492`，`deepsearch/domain/market_data/board.py:149`、`:195`                                                    | 与指标侧对齐板块命名规范，确认前台展示一致性                       |
| 5  | `galaxy::tgw::IGMDApi` 多进程登录冲突             | ✅ 配置强制走 `process` 模式并序列化登录，只保留单一 SDK 会话 | `deepsearch/config/settings.prod.yaml:114`，`deepsearch/infrastructure/providers/registry.py:421`、`:434`，`deepsearch/infrastructure/providers/implementations/amazingdata/amazingdata_process.py:227`、`:588`       | 发布后两周内复查登录成功率，必要时补充回归脚本                      |

> 结论：文档列出的 5 项问题均已在当前代码基线中落地修复，暂无新增整改事项。后续仅需按照表格中的例行监控计划持续跟踪即可。
