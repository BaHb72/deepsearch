# DeepSearch Codex 协作规范

AGENTS.md 仅用于约束 Codex 的行为，项目结构、运行与开发细节请参考 README.md。

## 交互基准

- 目前项目仓库里原本就存在大量本地改动未提交，一定谨慎。
- 项目有大量未提交内容，我们现阶段目的是为了提交一版达到提交标准无报错的代码，所以在大规模修改时请务必先仔细考虑影响规模后再进行。
- 所有回复与生成内容保持中文，确保与仓库约定一致。
- 严格遵循“系统 → 开发者 → 用户”指令优先级，发现指令冲突时立即求证。
- 未经用户明确允许，不得回滚 git、覆盖用户已有改动或删除历史记录。
- 所有 git 更改类操作（如 git commit、git restore、git cherry-pick 等，git add 仅在纳入新文件时例外）执行前须先征得用户同意。
- 注意保护用户与仓库数据，重大删除或敏感操作前需确认具备可恢复手段。
- 如检测到仓库存在本任务无关的异常改动，应立即暂停并询问用户处理方式。

## 命令执行

- 默认在 Windows PowerShell/CMD 中运行 `uv`、`python`、`npm` 等命令；WSL 仅可用于只读操作（如查看文件、搜索）。
- 使用仓库提供的虚拟环境：`./.venv/Scripts/Activate.ps1` 或在命令中显式传入 `--python ./.venv/Scripts/python.exe`。
- 非必要不要写入或依赖环境变量，配置统一维护在 `settings.<env>.yaml` 系列文件中。
- 执行命令前应评估副作用，避免无关的编译、安装或网络访问。

## 修改守则

- 遵守 README.md 中的“架构约束”“配置管理”“API 管理”“开发流程与质量”“贡献指南”等章节要求。
- 深入修改或新增功能前，先检查对应文档与测试目录，必要时同步更新文档与示例配置。
- 编写或更新文档时需核对上下文文字编码与文件实际编码保持一致，严禁引入乱码内容。
- QMT 相关脚本必须使用 GBK 编码并在首行声明 `# encoding:gbk`。
- AmazingData 为默认优先数据源，其他可用数据源按配置与现有 providers 策略决定；Mock 仅在测试环境验证稳定性，禁止向终端用户提供数据。

## 处理流程

- 代码或文档调整后需自检（包括但不限于 lint、测试、差异审阅），无法执行的检查需向用户说明原因。
- 与 README.md 内容不一致或缺失时，优先更新 README.md，并在最终回复中提示用户可能的后续操作。

## 参考

- 项目运行、测试、文档导航、提交规范等信息已整理至 README.md，对应章节应随变更及时更新。

